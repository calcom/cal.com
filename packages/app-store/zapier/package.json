{
  "$schema": "https://json.schemastore.org/package.json",
  "private": true,
  "name": "@calcom/zapier",
  "version": "0.0.0",
  "main": "./index.ts",
  "description": "Workflow automation for everyone. Use the Cal.com Zapier app to trigger your workflows when a booking is created, rescheduled, cancelled, or when there is a no-show.",
  "dependencies": {
    "@calcom/lib": "workspace:*",
    "node-schedule": "2.1.0"
  },
  "devDependencies": {
    "@calcom/types": "workspace:*",
    "@types/node-schedule": "2.1.0"
  }
}
