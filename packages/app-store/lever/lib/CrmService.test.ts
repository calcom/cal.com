import { describe, it, expect } from "vitest";

import CrmService from "./CrmService";

describe("Lever CrmService", () => {
    it("should be defined", () => {
      expect(CrmService).toBeDefined();
    });
});
