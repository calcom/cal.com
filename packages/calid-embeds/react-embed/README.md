# @calid/react-embed

React wrapper for embedding Cal ID.

Provides a React component that loads and controls the Cal embed runtime.

### Development

Start dev server (hot reload):

yarn dev

If testing inside another website, rebuild after changes:

yarn build

### Notes

Depends on @calid/embed-runtime

Intended to verify correct integration with the runtime

Runtime behavior is tested in the runtime package