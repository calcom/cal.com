{
  "extends": "@calcom/tsconfig/base.json",
  "compilerOptions": {
    // NodeNext is required to resolve ArkType's `arktype/config` entrypoint
    // Since "node" is deprecated, we should try and migrate the base config to "NodeNext" soon.
    "module": "NodeNext",
    "moduleResolution": "NodeNext"
  }
}
