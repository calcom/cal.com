export type TaskResponseType = {
  url_param: string;
  message: string;
};

export type RouterResponseType = {
  url_param: string;
  message: string;
};

export type ConversationResponseType = {
  text: string;
};
