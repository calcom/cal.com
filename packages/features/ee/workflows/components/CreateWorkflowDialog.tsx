import { Dialog, DialogContent } from "@calcom/ui";

export const CreateWorkflowDialog = () => {
  return (
    <Dialog name="new">
      <DialogContent type="creation" title="Create Workflow">
        <>Create Workflow Dialog</>
      </DialogContent>
    </Dialog>
  );
};
