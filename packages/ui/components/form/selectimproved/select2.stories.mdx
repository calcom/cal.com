import { Canvas, Meta, Story, ArgsTable } from "@storybook/addon-docs";

import {
  Examples,
  Example,
  Note,
  Title,
  CustomArgsTable,
  VariantRow,
  VariantsTable,
} from "@calcom/storybook/components";

import { FiPlus } from "../../icon";
import Select from "./components/Select";

<Meta title="UI/Form/CustomSelect" component={Select} />

<Title title="Select" suffix="Brief" subtitle="Version 2.0 — Last Update: 22 Aug 2022" />

export const options = [
  {
    label: "Cal.com Inc",
    options: [
      { value: "UserA", label: "Pro" },
      { value: "UserB", label: "Teampro" },
      { value: "UserC", label: "Example" },
      { value: "UserD", label: "Admin", disabled: true },
    ],
    label: "Acme Inc",
    options: [
      { value: "UserE", label: "Acme Pro" },
      { value: "UserF", label: "Acme Teampro" },
      { value: "UserG", label: "Acme example" },
      { value: "UserH", label: "Acme Admin", disabled: true },
    ],
  },
];

export const SelectWithState = (...args) => {
  const [value, setValue] = React.useState(options[0]);
  return (
    <Select options={options} selectedItems={value} onChange={(e) => setValue(e)} isSearchable isClearable />
  );
};

export const MultiWithState = (...args) => {
  const [value, setValue] = React.useState([options[0]]);
  return (
    <Select
      options={options}
      selectedItems={value}
      onChange={(e) => {
        setValue(e);
      }}
      isSearchable
      isMultiple
      isClearable
    />
  );
};

<Canvas>
  <Story name="Default">
    <div className="flex flex-col space-y-4">
      <SelectWithState />
      <MultiWithState />
    </div>
  </Story>
</Canvas>
