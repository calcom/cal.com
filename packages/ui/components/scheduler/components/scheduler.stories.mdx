import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs';
import { Examples, Example, Note, Title,CustomArgsTable,VariantsTable,VariantRow } from '@calcom/storybook/components'
import { Icon } from "@calcom/ui/Icon";
import {Scheduler} from "./Scheduler";
import {SchedulerEvent} from "../types/events"
import {Events,DEFAULT_SCHEDULE} from "../_storybookData"

<Meta title="UI/Scheduler/Scheduler" component={Scheduler} />

<Title title="Scheduler" suffix="Brief" subtitle="Version 2.0 — Last Update: 22 Aug 2022"/>

<Scheduler startHour={8} endHour={20} events={Events}  onEventClick={(e)=>alert(e.title)} onEmptyCellClick={(date)=>alert(date.toString())}/>

