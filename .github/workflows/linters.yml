name: Format Check
on:
  push:
    branches:
      - main

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - uses: ./.github/actions/yarn-install

      - name: Run Format Check
        run: yarn fmtcheck
