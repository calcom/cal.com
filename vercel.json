{
  "buildCommand": "npm run db-deploy && npm run build",
  "functions": {
    "apps/web/pages/api/**/*.js": {
      "maxDuration": 30
    }
  },
  "framework": "nextjs",
  "installCommand": "yarn install",
  "buildSettings": {
    "installCommand": "yarn install",
    "buildCommand": "yarn workspace @calcom/web build",
    "outputDirectory": "apps/web/.next"
  }
}
