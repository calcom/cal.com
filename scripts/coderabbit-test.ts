// Temporary scratch file to verify CodeRabbit is reviewing PRs on this fork.
// Safe to delete/close this PR without merging once confirmed.

function getDiscountedPrice(price: number) {
  // Get the discounted price
  const discounted = price - price * 0.15;
  console.log(discounted);
  return discounted;
}

export { getDiscountedPrice };
