---
omitFromSearch: true
---

import { useEffect } from "react"

export const Redirect = () => {
  useEffect(() => {
    {/* Only perform the redirect on the published site (no need in the preview) */}
    if (window.location.pathname?.startsWith('/docs/core-features/event-types/require-additional-notes')) {
      window.location.href = "https://cal.com/docs/core-features/event-types/booking-questions";
    }
  }, [])
  
  return <></>
}

<Redirect />