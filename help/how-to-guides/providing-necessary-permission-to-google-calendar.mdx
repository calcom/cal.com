---
omitFromSearch: true
---

import { useEffect } from "react"

export const Redirect = () => {
  useEffect(() => {
    {/* Only perform the redirect on the published site (no need in the preview) */}
    if (window.location.pathname?.startsWith('/docs/how-to-guides/providing-necessary-permission-to-google-calendar')) {
      window.location.href = "https://cal.com/docs/how-to-guides/calendar/providing-necessary-permission-to-google-calendar";
    }
  }, [])
  
  return <></>
}

<Redirect />
