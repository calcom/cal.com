---
omitFromSearch: true
---

import { useEffect } from "react"

export const Redirect = () => {
  useEffect(() => {
    {/* Only perform the redirect on the published site (no need in the preview) */}
    if (window.location.pathname?.startsWith('/docs/how-to-guides/setting-up-mailtrap-for-email-testing')) {
      window.location.href = "https://cal.com/docs/how-to-guides/self-hosting/setting-up-mailtrap-for-email-testing";
    }
  }, [])
  
  return <></>
}

<Redirect />
