---
omitFromSearch: true
---

import { useEffect } from "react"

export const Redirect = () => {
  useEffect(() => {
    {/* Only perform the redirect on the published site (no need in the preview) */}
    if (window.location.pathname?.startsWith('/docs/introduction/quick-start/self-hosting/installation/platform/get-access-to-the-api')) {
      window.location.href = "https://cal.com/docs/introduction/quick-start/self-hosting/installation";
    }
  }, [])
  
  return <></>
}

<Redirect />