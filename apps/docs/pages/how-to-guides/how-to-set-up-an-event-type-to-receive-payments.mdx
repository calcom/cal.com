---
omitFromSearch: true
---

import { useEffect } from "react"

export const Redirect = () => {
  useEffect(() => {
    {/* Only perform the redirect on the published site (no need in the preview) */}
    if (window.location.pathname?.startsWith('/docs/how-to-guides/how-to-set-up-an-event-type-to-receive-payments')) {
      window.location.href = "https://cal.com/docs/how-to-guides/event-types/how-to-create-paid-events";
    }
  }, [])
  
  return <></>
}

<Redirect />
