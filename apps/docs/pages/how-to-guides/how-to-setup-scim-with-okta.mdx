---
omitFromSearch: true
---

import { useEffect } from "react"

export const Redirect = () => {
  useEffect(() => {
    {/* Only perform the redirect on the published site (no need in the preview) */}
    if (window.location.pathname?.startsWith('/docs/how-to-guides/how-to-setup-scim-with-okta')) {
      window.location.href = "https://cal.com/docs/how-to-guides/auth-and-provision/how-to-setup-scim-with-okta";
    }
  }, [])
  
  return <></>
}

<Redirect />
