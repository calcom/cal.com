---
omitFromSearch: true
---

import { useEffect } from "react"

export const Redirect = () => {
  useEffect(() => {
    {/* Only perform the redirect on the published site (no need in the preview) */}
    if (window.location.pathname?.startsWith('/docs/how-to-guides/how-to-troubleshoot-symbolic-link-issues-on-windows')) {
      window.location.href = "https://cal.com/docs/troubleshooting-guides/self-hosting/symbolic-link-issues-on-windows";
    }
  }, [])
  
  return <></>
}

<Redirect />
