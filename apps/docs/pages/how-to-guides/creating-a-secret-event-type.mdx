---
omitFromSearch: true
---

import { useEffect } from "react"

export const Redirect = () => {
  useEffect(() => {
    {/* Only perform the redirect on the published site (no need in the preview) */}
    if (window.location.pathname?.startsWith('/docs/how-to-guides/creating-a-secret-event-type')) {
      window.location.href = "https://cal.com/docs/how-to-guides/event-types/creating-a-secret-event-type";
    }
  }, [])
  
  return <></>
}

<Redirect />
