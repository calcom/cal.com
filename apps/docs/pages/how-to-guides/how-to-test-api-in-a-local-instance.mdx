---
omitFromSearch: true
---

import { useEffect } from "react"

export const Redirect = () => {
  useEffect(() => {
    {/* Only perform the redirect on the published site (no need in the preview) */}
    if (window.location.pathname?.startsWith('/docs/how-to-guides/how-to-test-api-in-a-local-instance')) {
      window.location.href = "https://cal.com/docs/how-to-guides/self-hosting/how-to-set-up-api-in-a-local-instance";
    }
  }, [])
  
  return <></>
}

<Redirect />
