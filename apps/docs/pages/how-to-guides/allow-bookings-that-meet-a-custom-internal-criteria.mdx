---
omitFromSearch: true
---

import { useEffect } from "react"

export const Redirect = () => {
  useEffect(() => {
    {/* Only perform the redirect on the published site (no need in the preview) */}
    if (window.location.pathname?.startsWith('/docs/how-to-guides/allow-bookings-that-meet-a-custom-internal-criteria')) {
      window.location.href = "https://cal.com/docs/how-to-guides/bookings/allow-bookings-that-meet-a-custom-internal-criteria";
    }
  }, [])
  
  return <></>
}

<Redirect />
