---
omitFromSearch: true
---

import { useEffect } from "react"

export const Redirect = () => {
  useEffect(() => {
    {/* Only perform the redirect on the published site (no need in the preview) */}
    if (window.location.pathname?.startsWith('/docs/how-to-guides/acquire-and-manage-a-license-key')) {
      window.location.href = "https://cal.com/docs/how-to-guides/self-hosting/acquire-and-manage-a-license-key";
    }
  }, [])
  
  return <></>
}

<Redirect />
