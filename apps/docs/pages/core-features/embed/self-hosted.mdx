---
omitFromSearch: true
---

import { useEffect } from "react"

export const Redirect = () => {
  useEffect(() => {
    {/* Only perform the redirect on the published site (no need in the preview) */}
    if (window.location.pathname?.startsWith('/docs/core-features/embed/self-hosted')) {
      window.location.href = "https://cal.com/docs/core-features/embed/adding-embed-to-your-webpage";
    }
  }, [])
  
  return <></>
}

<Redirect />