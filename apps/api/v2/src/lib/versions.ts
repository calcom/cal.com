import { VersionValue } from "@nestjs/common/interfaces";

import { API_VERSIONS } from "@calcom/platform-constants";

export const API_VERSIONS_VALUES: VersionValue = API_VERSIONS as unknown as VersionValue;
