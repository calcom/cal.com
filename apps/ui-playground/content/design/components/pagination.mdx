---
title: Pagination
---

import { BasicExample, CustomPageSizesExample, LargeDatasetExample, WithCallbacksExample } from "./pagination";

# Pagination

The Pagination component provides a consistent way to handle data pagination across the application. It includes rows per page selection and navigation controls.

## Installation

```ts
import { Pagination } from "@calcom/ui";
```

## Examples

### Basic Usage

A basic pagination component with default settings.

<BasicExample />

### Custom Page Sizes

Customize the available page size options.

<CustomPageSizesExample />

### With Navigation Callbacks

Use `onNext` and `onPrevious` callbacks to perform additional actions when navigating pages.

<WithCallbacksExample />

### Large Dataset

The pagination component handles large datasets efficiently.

<LargeDatasetExample />

